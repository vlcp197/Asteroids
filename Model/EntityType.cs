
namespace Asteroids.Model
{
    // I don't know why this is implemented, but as soon as i discover i will change that comment.
    public enum EntityType
    {
        None = 0,
        Player = 1,
        Asteroid = 2,
        LaserProjectile = 3
    }
}
